pipeline {
    agent {
        docker {
            image 'node:18-alpine'
            args '-u root -v /var/lib/docker/volumes/jenkins_home/_data/workspace/frontend/angular/dist/BookStore:/dist/BookStore'
        }
    }

    parameters {
        string(name: "GIT_BRANCH", defaultValue: "develop", description: "Branch to build")
    }

    environment {
        ANGULAR_DIR = "angular"
    }

    stages {
        stage("Checkout") {
            steps {
                git branch: "${params.GIT_BRANCH}", credentialsId: "ssh-git", url: "git@github.com:TranNguyenPhucAnh/Acme.BookStore.git"
            }
        }

        stage("Install & Build Angular") {
            steps {
                dir("${ANGULAR_DIR}") {
                    sh """
                        corepack enable yarn
                        yarn install
                        yarn build:prod
                        cp dynamic-env.json /dist/BookStore/dynamic-env.json
                        """
                }
            }
        }

        stage("Archive Build") {
            steps {
                archiveArtifacts artifacts: "${ANGULAR_DIR}/dist/**", fingerprint: true
            }
        }
    }

    post {
        success {
            echo "Build & Deploy succeeded!"
        }
        failure {
            echo "Build failed!"
            emailext (
                subject: "Build ${env.JOB_NAME} #${env.BUILD_NUMBER} Failed",
                body: """Build ${env.JOB_NAME} #${env.BUILD_NUMBER} failed.
                    Branch: ${params.GIT_BRANCH}
                    View details: ${env.BUILD_URL}""",
                to: "phucanhatt@gmail.com",
                from: "support@mynginx.store"
            )
        }
    }
}